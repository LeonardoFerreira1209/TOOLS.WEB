import { Basic } from './basic.js';
/**
 * Animation with auto start and loop.
 */
export declare class Loop extends Basic {
    playDelay: any;
    ready(): void;
    disconnectedCallback(): void;
    complete(): void;
    resetPlayDelayTimer(): void;
    get delay(): number;
}
